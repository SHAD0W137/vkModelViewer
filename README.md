# vkModelViewer
A take at making a Model Viewer with Vulkan API. Potentially as a replacement to retiring Windows viewer

# Project Strcuture
Generated folders:
```/build``` folder is the build target folder
```/external``` folder contains all dependencies pulled from github with FetchContent
